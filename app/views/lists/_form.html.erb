<%= simple_form_for(@list) do |f| %>
  <%= f.input :name, label: "Please add a list name:" %>
  <%= f.input :image_url, label: "Please insert an image url:" %>
  <%= f.submit class: "btn btn-flat mb-3" %>
<% end %>
